---
phase: 14-contact-provenance-tracking-for-clean-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mailroom/core/config.py
  - src/mailroom/setup/provisioner.py
  - src/mailroom/setup/sieve_guidance.py
  - src/mailroom/workflows/screener.py
  - src/mailroom/__main__.py
  - config.yaml.example
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_provisioner.py
  - tests/test_sieve_guidance.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03

must_haves:
  truths:
    - "Config uses `mailroom:` section with `label_error`, `label_warning`, `warnings_enabled`, and `provenance_group` keys"
    - "Old `labels:` config key causes app to fail to start (no backward compatibility)"
    - "Setup CLI creates provenance group as kind=mailroom resource"
    - "Provenance group is validated at startup alongside triage groups"
    - "Sieve guidance does NOT mention provenance group"
  artifacts:
    - path: "src/mailroom/core/config.py"
      provides: "MailroomSectionSettings model with provenance_group field"
      contains: "class MailroomSectionSettings"
    - path: "config.yaml.example"
      provides: "Updated config with mailroom: section"
      contains: "mailroom:"
    - path: "tests/test_config.py"
      provides: "Config rename + provenance_group tests"
  key_links:
    - from: "src/mailroom/core/config.py"
      to: "src/mailroom/workflows/screener.py"
      via: "settings.mailroom.label_error, settings.mailroom.label_warning"
      pattern: "settings\\.mailroom\\."
    - from: "src/mailroom/core/config.py"
      to: "src/mailroom/setup/provisioner.py"
      via: "settings.mailroom.label_error"
      pattern: "settings\\.mailroom\\."
    - from: "src/mailroom/__main__.py"
      to: "src/mailroom/clients/carddav.py"
      via: "validate_groups includes provenance group"
      pattern: "validate_groups"
---

<objective>
Rename config `labels:` section to `mailroom:`, add provenance_group field, update all references across the codebase, update setup provisioner to create provenance group, and ensure provenance group is validated at startup.

Purpose: Establishes the config foundation and infrastructure provisioning that all subsequent plans depend on. The config rename is a cross-cutting change that must land first.
Output: Updated config model, updated provisioner, updated all settings.labels references, updated tests.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-contact-provenance-tracking-for-clean-reset/14-CONTEXT.md
@.planning/phases/14-contact-provenance-tracking-for-clean-reset/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/mailroom/core/config.py (CURRENT — will be renamed):
```python
class LabelSettings(BaseModel):
    mailroom_error: str = "@MailroomError"
    mailroom_warning: str = "@MailroomWarning"
    warnings_enabled: bool = True

class MailroomSettings(BaseSettings):
    labels: LabelSettings = LabelSettings()
```

All references to update (grep-verified):
- src/mailroom/workflows/screener.py:178 — settings.labels.mailroom_error
- src/mailroom/workflows/screener.py:279 — settings.labels.mailroom_error
- src/mailroom/workflows/screener.py:328 — settings.labels.mailroom_warning
- src/mailroom/workflows/screener.py:419 — settings.labels.warnings_enabled
- src/mailroom/core/config.py:397 — self.labels.mailroom_error
- src/mailroom/core/config.py:402-403 — self.labels.warnings_enabled, self.labels.mailroom_warning
- src/mailroom/setup/provisioner.py:58-60 — settings.labels.*
- tests/test_config.py:32-34, 104-105, 361 — settings.labels.*
- tests/test_screener_workflow.py:1568 — mock_settings.labels.warnings_enabled

From src/mailroom/__main__.py:128:
```python
carddav.validate_groups(settings.contact_groups)
```

From src/mailroom/setup/provisioner.py:
```python
mailroom_names = {settings.labels.mailroom_error}
if settings.labels.warnings_enabled:
    mailroom_names.add(settings.labels.mailroom_warning)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config model rename and provenance_group field</name>
  <files>
    src/mailroom/core/config.py,
    config.yaml.example,
    tests/test_config.py,
    tests/conftest.py
  </files>
  <behavior>
    - Test: MailroomSettings parses `mailroom:` section with `label_error`, `label_warning`, `warnings_enabled`, `provenance_group` keys
    - Test: Default provenance_group is "Mailroom"
    - Test: `required_mailboxes` uses `self.mailroom.label_error` and `self.mailroom.label_warning`
    - Test: `contact_groups` property does NOT include provenance_group (it is infrastructure, not triage)
    - Test: Config with old `labels:` key is silently ignored (pydantic ignores unknown fields) — but `mailroom:` section must be present for non-default values
  </behavior>
  <action>
    1. In config.py, rename `LabelSettings` to `MailroomSectionSettings`. Rename fields:
       - `mailroom_error` -> `label_error` (default "@MailroomError")
       - `mailroom_warning` -> `label_warning` (default "@MailroomWarning")
       - `warnings_enabled` stays as-is
       - Add `provenance_group: str = "Mailroom"` field
    2. In MailroomSettings, change `labels: LabelSettings = LabelSettings()` to `mailroom: MailroomSectionSettings = MailroomSectionSettings()`
    3. Update `required_mailboxes` property: `self.labels.mailroom_error` -> `self.mailroom.label_error`, etc.
    4. Update config.yaml.example: rename `labels:` to `mailroom:`, rename keys, add `provenance_group: "Mailroom"`
    5. Update tests/conftest.py: any mock_settings fixtures that reference `labels` must use `mailroom`
    6. Update tests/test_config.py: all assertions using `settings.labels.*` -> `settings.mailroom.*`
    7. Note: `contact_groups` property must NOT include `provenance_group` — that property drives triage pipeline group validation only
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Services/mailroom && python -m pytest tests/test_config.py -x -q</automated>
  </verify>
  <done>
    Config model uses MailroomSectionSettings with renamed fields and provenance_group. config.yaml.example uses mailroom: section. All config tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update all settings.labels references + setup provisioner + startup validation</name>
  <files>
    src/mailroom/workflows/screener.py,
    src/mailroom/setup/provisioner.py,
    src/mailroom/__main__.py,
    src/mailroom/reset/resetter.py,
    tests/test_provisioner.py,
    tests/test_screener_workflow.py,
    tests/test_sieve_guidance.py
  </files>
  <behavior>
    - Test: ScreenerWorkflow uses settings.mailroom.label_error, settings.mailroom.label_warning, settings.mailroom.warnings_enabled
    - Test: Setup provisioner creates provenance group as kind="mailroom" resource
    - Test: Provisioner uses settings.mailroom.label_error and settings.mailroom.label_warning
    - Test: Sieve guidance does NOT mention provenance group
  </behavior>
  <action>
    1. In screener.py, update all 4 references:
       - Line 178: `self._settings.labels.mailroom_error` -> `self._settings.mailroom.label_error`
       - Line 279: same change
       - Line 328: `self._settings.labels.mailroom_warning` -> `self._settings.mailroom.label_warning`
       - Line 419: `self._settings.labels.warnings_enabled` -> `self._settings.mailroom.warnings_enabled`
    2. In provisioner.py plan_resources(), update lines 58-60:
       - `settings.labels.mailroom_error` -> `settings.mailroom.label_error`
       - `settings.labels.warnings_enabled` -> `settings.mailroom.warnings_enabled`
       - `settings.labels.mailroom_warning` -> `settings.mailroom.label_warning`
       - Add provenance group to mailroom_names resource list: add a new contact_group ResourceAction with kind="mailroom" for `settings.mailroom.provenance_group`
    3. In __main__.py line 128, extend validate_groups to include provenance group:
       `carddav.validate_groups(settings.contact_groups + [settings.mailroom.provenance_group])`
    4. In resetter.py run_reset() line 296, extend validate_groups similarly:
       `carddav.validate_groups(settings.contact_groups + [settings.mailroom.provenance_group])`
    5. Update tests/test_screener_workflow.py: line 1568 `mock_settings.labels.warnings_enabled` -> `mock_settings.mailroom.warnings_enabled`
    6. Update tests/test_provisioner.py: verify provenance group appears in plan_resources output as kind="mailroom"
    7. Verify sieve guidance (tests/test_sieve_guidance.py) does NOT output provenance group name — no changes needed if sieve_guidance.py uses `resolved_categories` (which excludes provenance)
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Services/mailroom && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    All settings.labels references updated to settings.mailroom. Setup provisioner creates provenance group. Startup validates provenance group. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` — full test suite passes
- `grep -r "settings\.labels" src/ tests/` returns zero results (all references migrated)
- `grep "provenance_group" src/mailroom/core/config.py` shows the field exists with default "Mailroom"
- `grep "provenance_group" src/mailroom/setup/provisioner.py` shows it in resource planning
</verification>

<success_criteria>
- MailroomSectionSettings replaces LabelSettings with renamed fields + provenance_group
- All `settings.labels.*` references migrated to `settings.mailroom.*`
- Setup provisioner creates provenance group as kind="mailroom"
- Startup validates provenance group alongside triage groups
- Sieve guidance ignores provenance group
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/14-contact-provenance-tracking-for-clean-reset/14-01-SUMMARY.md`
</output>
