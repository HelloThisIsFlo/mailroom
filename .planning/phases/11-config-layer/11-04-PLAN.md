---
phase: 11-config-layer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mailroom/core/config.py
  - src/mailroom/__main__.py
  - tests/test_config.py
  - src/mailroom/cli.py
  - src/mailroom/setup/provisioner.py
  - src/mailroom/setup/sieve_guidance.py
  - tests/test_sieve_guidance.py
  - human-tests/test_14_setup_dry_run.py
  - human-tests/test_15_setup_apply.py
autonomous: true
requirements: [CFG-02, CFG-08]
gap_closure: true

must_haves:
  truths:
    - "destination_mailbox set to 'inbox', 'INBOX', or any case variant is rejected with the same helpful error as 'Inbox'"
    - "ValidationError from config loading is caught and displayed as a clean one-line message, not a raw Pydantic traceback"
    - "Sieve guidance has no --ui-guide flag, no _build_ui_guide function, and no commented sieve equivalent blocks"
    - "generate_sieve_guidance() takes only settings (no ui_guide parameter)"
  artifacts:
    - path: "src/mailroom/core/config.py"
      provides: "Case-insensitive CFG-02 check"
      contains: "resolved_mailbox.lower()"
    - path: "src/mailroom/__main__.py"
      provides: "Clean ValidationError handling"
      contains: "ValidationError"
    - path: "src/mailroom/setup/sieve_guidance.py"
      provides: "Cleaned sieve guidance without ui_guide or commented blocks"
    - path: "src/mailroom/cli.py"
      provides: "Setup command without --ui-guide option"
  key_links:
    - from: "src/mailroom/core/config.py"
      to: "tests/test_config.py"
      via: "case-insensitive validation test"
      pattern: "inbox.*INBOX"
    - from: "src/mailroom/setup/sieve_guidance.py"
      to: "src/mailroom/setup/provisioner.py"
      via: "generate_sieve_guidance call signature"
      pattern: "generate_sieve_guidance\\(settings\\)"
---

<objective>
Close two UAT gaps from Phase 11: (1) CFG-02 case-insensitive Inbox rejection with clean error output, (2) remove --ui-guide flag and commented sieve blocks from sieve guidance.

Purpose: Both gaps are minor polish issues reported during UAT. The CFG-02 gap means lowercase 'inbox' bypasses the helpful error and fails later with a confusing message. The sieve gap leaves dead code and clutter in the output.
Output: Clean config validation, clean CLI output, clean sieve guidance.
</objective>

<execution_context>
@/Users/flo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-config-layer/11-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CFG-02 case-insensitive Inbox rejection with clean error output</name>
  <files>src/mailroom/core/config.py, src/mailroom/__main__.py, tests/test_config.py</files>
  <behavior>
    - Test: destination_mailbox="inbox" (lowercase) raises ValueError with "add_to_inbox" in message
    - Test: destination_mailbox="INBOX" (uppercase) raises ValueError with "add_to_inbox" in message
    - Test: category named "inbox" (derives destination_mailbox="inbox") raises ValueError
    - Test: existing "Inbox" test still passes (exact case)
    - Test: MailroomSettings() with invalid config prints clean error message to stderr (not raw Pydantic traceback)
  </behavior>
  <action>
    1. In `src/mailroom/core/config.py` line 193, change `resolved_mailbox == "Inbox"` to `resolved_mailbox.lower() == "inbox"`. This is the only change needed in config.py -- the comparison becomes case-insensitive.

    2. In `tests/test_config.py`, add two new test methods to `TestDestinationMailboxInboxRejected`:
       - `test_lowercase_inbox_rejected`: uses `TriageCategory(name="Imbox", destination_mailbox="inbox")`, expects ValueError with "add_to_inbox" in message.
       - `test_uppercase_inbox_rejected`: uses `TriageCategory(name="Imbox", destination_mailbox="INBOX")`, expects ValueError with "add_to_inbox" in message.
       - `test_derived_lowercase_inbox_rejected`: uses `TriageCategory(name="inbox")` (lowercase category name derives destination_mailbox="inbox"), expects ValueError.

    3. In `src/mailroom/__main__.py`, wrap `settings = MailroomSettings()` (line 103) in a try/except that catches `pydantic.ValidationError` and prints a clean message:
       ```python
       from pydantic import ValidationError
       try:
           settings = MailroomSettings()
       except ValidationError as exc:
           import sys
           print(f"Configuration error: {exc}", file=sys.stderr)
           sys.exit(1)
       ```
       Also catch the plain `ValueError` that `resolve_categories` raises (it is raised inside a Pydantic validator, so it will be wrapped in ValidationError -- but verify this). The goal: user sees a readable error, not a raw traceback.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Services/mailroom && uv run pytest tests/test_config.py::TestDestinationMailboxInboxRejected -x -v</automated>
  </verify>
  <done>destination_mailbox="inbox", "INBOX", and "Inbox" all raise the same helpful error; __main__.py catches ValidationError and prints clean output</done>
</task>

<task type="auto">
  <name>Task 2: Remove --ui-guide flag and commented sieve blocks</name>
  <files>src/mailroom/cli.py, src/mailroom/setup/provisioner.py, src/mailroom/setup/sieve_guidance.py, tests/test_sieve_guidance.py, human-tests/test_14_setup_dry_run.py, human-tests/test_15_setup_apply.py</files>
  <action>
    1. In `src/mailroom/setup/sieve_guidance.py`:
       - Remove `ui_guide` parameter from `generate_sieve_guidance()` signature (line 19). It should just be `def generate_sieve_guidance(settings: MailroomSettings) -> str:`.
       - Remove the `if ui_guide:` branch (lines 41-42) and the `return` line that calls `_build_ui_guide`.
       - Delete the entire `_build_ui_guide()` function (lines 144-208).
       - In `_build_sieve_snippets()`, delete the commented sieve equivalent blocks. These are lines 109-116 (after root rule) and lines 122-129 (after child rule). They start with `# Emit sieve reference for root` / empty line and contain `# Fastmail sieve equivalent`, `# require`, `# if jmapquery`, `# fileinto` lines.
       - The function should just call `return _build_sieve_snippets(roots, children_of, settings.triage.screener_mailbox)` directly (no branching).

    2. In `src/mailroom/setup/provisioner.py`:
       - Remove `ui_guide` parameter from `run_setup()` signature (line 191). It should be `def run_setup(apply: bool = False) -> int:`.
       - Update both calls to `generate_sieve_guidance()` (lines 252 and 259) to remove the `ui_guide=ui_guide` argument. They become `generate_sieve_guidance(settings)`.
       - Update the docstring to remove the `ui_guide` parameter description.

    3. In `src/mailroom/cli.py`:
       - Remove the `@click.option("--ui-guide", ...)` decorator (lines 30-32).
       - Remove `ui_guide` from the `setup()` function signature (line 33). It becomes `def setup(apply: bool) -> None:`.
       - Remove `ui_guide=ui_guide` from the `run_setup()` call (line 37). It becomes `run_setup(apply=apply)`.

    4. In `tests/test_sieve_guidance.py`:
       - Delete the entire `TestGenerateGuidanceUIGuideMode` class (lines 151-196).
       - Delete `test_sieve_reference_snippets` (lines 71-75) since the commented sieve blocks no longer exist.
       - Update all remaining `generate_sieve_guidance(settings, ui_guide=False)` calls to just `generate_sieve_guidance(settings)`.

    5. In `human-tests/test_14_setup_dry_run.py`:
       - Change `run_setup(apply=False, ui_guide=False)` to `run_setup(apply=False)` (line 92).

    6. In `human-tests/test_15_setup_apply.py`:
       - Change `run_setup(apply=True, ui_guide=False)` to `run_setup(apply=True)` at lines 42 and 129.
  </action>
  <verify>
    <automated>cd /Users/flo/Work/Private/Dev/Services/mailroom && uv run pytest tests/test_sieve_guidance.py tests/test_config.py -x -v</automated>
  </verify>
  <done>No --ui-guide flag in CLI, no ui_guide parameter anywhere in codebase, no _build_ui_guide function, no commented sieve equivalent blocks in output, all tests pass</done>
</task>

</tasks>

<verification>
1. `uv run pytest` -- all tests pass (count will decrease by ~7 due to deleted UI guide tests and sieve snippet test)
2. `uv run python -m mailroom setup` -- sieve guidance output has no commented sieve blocks, no mention of jmapquery or fileinto
3. `uv run python -m mailroom setup --ui-guide` -- should fail with "No such option: --ui-guide"
4. Config with `destination_mailbox: inbox` (lowercase) shows clean error message, not traceback
</verification>

<success_criteria>
- All pytest tests pass
- CFG-02 rejects "inbox", "INBOX", "Inbox" and all case variants with the same helpful error
- ValidationError from config is shown as clean message in __main__.py
- --ui-guide option is completely removed from CLI, provisioner, sieve_guidance, and tests
- Sieve guidance output contains no commented sieve code blocks (no fileinto, no jmapquery)
</success_criteria>

<output>
After completion, create `.planning/phases/11-config-layer/11-04-SUMMARY.md`
</output>
