---
status: complete
phase: 11-config-layer
source: 11-01-SUMMARY.md, 11-02-SUMMARY.md, 11-03-SUMMARY.md
started: 2026-03-02T20:00:00Z
updated: 2026-03-03T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. All Tests Pass
expected: Running `uv run pytest` passes all tests (318 expected). No failures, no errors.
result: pass

### 2. Seven Default Categories
expected: Config resolves 7 default categories: Imbox, Feed, Paper Trail, Jail, Person, Billboard, Truck.
result: pass

### 3. Independent Child Categories
expected: Person has contact_group="Person", destination_mailbox="Person" (not inherited from Imbox). Billboard has contact_group="Billboard", destination_mailbox="Billboard" (not inherited from Paper Trail).
result: pass

### 4. CFG-02: Inbox as Destination Rejected
expected: Setting destination_mailbox: Inbox on any category raises a validation error pointing to add_to_inbox: true instead. Should catch both "Inbox" and "inbox" (case-insensitive).
result: issue
reported: "The error message is good but buried in a long Python/Pydantic traceback — hard to find. Also, lowercase 'inbox' is not caught by CFG-02 — it fails later with a confusing 'Required mailboxes not found in Fastmail' error instead of the helpful message. Should be case-insensitive."
severity: minor

### 5. Additive Mailbox Filing
expected: Person triage files to both Person AND Imbox mailboxes. Billboard files to both Billboard AND Paper Trail.
result: pass

### 6. Additive Contact Groups
expected: Person triage adds sender to both Person AND Imbox contact groups. Billboard adds to both Billboard AND Paper Trail groups.
result: pass

### 7. add_to_inbox Per-Category Only
expected: Imbox add_to_inbox=True adds Inbox label. Person (child of Imbox) does NOT inherit — Person triage does NOT add Inbox label.
result: pass

### 8. Sieve Guidance Output
expected: All 7 categories with syntax highlighting, grouped by parent, add_to_inbox annotation, IMPORTANT note at top.
result: issue
reported: "Two issues: (1) --ui-guide option is outdated and its output is not as good as the default — should be removed. (2) The commented sieve equivalent code blocks (e.g. '# Fastmail sieve equivalent (generated by the UI): ...') are clutter — either do it programmatically (future milestone) or just show the UI instructions (which the default does wonderfully). Remove these commented code blocks."
severity: minor

### 9. Config Example Updated
expected: config.yaml.example includes add_to_inbox field, Billboard and Truck categories, independent children comments, additive-only explanation.
result: pass

## Summary

total: 9
passed: 7
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "CFG-02 catches destination_mailbox: Inbox case-insensitively with clear error"
  status: failed
  reason: "User reported: error message buried in traceback; lowercase 'inbox' not caught by CFG-02, fails later with confusing 'mailboxes not found' error instead"
  severity: minor
  test: 4
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""

- truth: "Sieve guidance output is clean and focused on UI instructions"
  status: failed
  reason: "User reported: (1) --ui-guide option outdated, should be removed. (2) Commented sieve equivalent code blocks are clutter, should be removed."
  severity: minor
  test: 8
  root_cause: ""
  artifacts: []
  missing: []
  debug_session: ""
