# Copy this to .env and fill in your Fastmail API token:
#   cp .env.example .env
#
# Get a token: Fastmail → Settings → Privacy & Security → API tokens
# Permissions needed: Email (read+write), Contacts (read)
MAILROOM_JMAP_TOKEN=fmu1-your-token-here
