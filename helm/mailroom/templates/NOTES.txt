Mailroom deployed to namespace {{ .Release.Namespace }}.
{{- if .Values.setup.apply }}
Setup ran in apply mode — Fastmail resources have been provisioned.
{{- end }}

Check status:
  kubectl rollout status deployment/{{ include "mailroom.fullname" . }} -n {{ .Release.Namespace }}
  kubectl logs -n {{ .Release.Namespace }} deploy/{{ include "mailroom.fullname" . }}
  kubectl logs -n {{ .Release.Namespace }} job/{{ include "mailroom.fullname" . }}-setup
