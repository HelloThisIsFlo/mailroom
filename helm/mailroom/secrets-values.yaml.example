# Mailroom Secrets
#
# Usage:
#   1. Copy this file:  cp secrets-values.yaml.example secrets-values.yaml
#   2. Replace placeholder values with your real Fastmail credentials
#   3. Pass to Helm:  helm install mailroom helm/mailroom/ -n mailroom -f secrets-values.yaml
#   4. Do NOT commit secrets-values.yaml to git (it is in .gitignore)
#
secrets:
  jmap_token: "your-fastmail-jmap-token-here"
  carddav_username: "your-fastmail-email@fastmail.com"
  carddav_password: "your-fastmail-app-password-here"
